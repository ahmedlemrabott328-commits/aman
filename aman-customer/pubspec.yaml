name: aman_customer
description: تطبيق الزبون — منصة AMAN للنقل الذكي
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0

  dio: ^5.5.0
  pretty_dio_logger: ^1.3.1

  flutter_secure_storage: ^9.2.2

  pusher_channels_flutter: ^2.4.1

  geolocator: ^13.0.1
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  geocoding: ^3.0.0

  firebase_messaging: ^15.1.0
  flutter_local_notifications: ^17.2.2

  google_fonts: ^6.2.1

  intl: ^0.19.0
  url_launcher: ^6.3.0
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
